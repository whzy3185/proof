# JCTA verification snapshot

This repository state is intended to be frozen for the JCTA submission corresponding to manuscript v1.21.

The proof-critical data are checked in under `data/`; CI is read-only and verifies both the archived hashes and the full exact proof computations.

Recommended release tag: `v1.0.0-jcta-submission`.
